import XCTest
@testable import NavigationButtonable

final class NavigationButtonableTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(NavigationButtonable().text, "Hello, World!")
    }
}
